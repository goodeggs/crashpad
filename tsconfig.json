{
  "extends": "@goodeggs/tsconfig/base",
  "compilerOptions": {
    "downlevelIteration": true,
    // Chance is not typed in a way that allows you to import the type for an instance
    "noUnusedLocals": false
  }
}